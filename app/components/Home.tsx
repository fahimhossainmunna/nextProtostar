import Counter from "../(user)/counter/Counter";
import Hero from "../(user)/hero/Hero";

const Home = () => {
  return (
    <div>
      <Hero/>
      <Counter/>
    </div>
  );
};

export default Home;
